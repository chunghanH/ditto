#install homebrew
/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"

#install python
brew install python

#install virtualenv
pip3 install virtualenv

#create virtual enviroment
virtualenv venv

#Windows to virtualenv
venv\scripts\activate

#OSX to virtualenv
source venv/bin/activate

#install django
pip3 install django

#create project
django-admin startproject project_name

#create application
python manage.py startapp app_name
#add applicatoin into settings.py(INSTALLED_APPS)

#make migration
python manage.py migrate
python manage.py makemigrations app_name

#CBV Template view
from django import forms
from gallery.models import Work

class UploadFileForm(forms.ModelForm):
    class Meta:
        model = Work
        fields = "__all__"

#implement model gets related object(many to one)
reporters = Reporter.objects.all()
for reporter in reporters:
    for article in reporter.article_set.all()
        print(article.title)

#install create-react-app
npm install -g create-react-app
create-react-app app_name
#install react-router-dom
npm install --save react-router-dom
＃instal bootstrap
npm install --save bootstrap
# install axios
npm install axios

#Django + React.js + Webpack
after create django project, create app name frontend
create react project name templates inside the frontend app
install router
install bootstrap
settings.py set template & static dir
fronend views.py create entrypoint page mehod




#install webpack
npm init
npm install webpack webpack-cli --save-dev
npm install --save-dev babel-core babel-eslint babel-loader babel-preset-es2015 babel-preset-react

#install babel
npm install babel-loader @babel/core @babel/preset-env --save-dev

install vue-cli
npm install -g @vue/cli



